# Premium startup header

Fresh Pi startup captured at 150×46 columns/rows.

- `final.png` / `final.txt`: final wide layout with the full six-line `PI CODING AGENT` gradient wordmark.
- `session-final.termctrl`: interactive terminal recording.

The header shows the selected agent configuration, role/model/thinking matrix, actual active context filenames, commands, loaded skills, and shortcuts. The old Setup capability copy and dot-separated lists are removed. Wide terminals use paired editorial columns; narrower terminals use a compact logo and stacked sections.

Forge is `openai-codex/gpt-5.6-sol` with `medium` thinking in project, bundled, and global configurations.

Verification: 36/36 tests passed, TypeScript typecheck passed, and all Terminal Control sessions were stopped.
