# Structured startup screen

`fresh-start.*` verifies the globally active setup at 140×44:

- gradient PI ASCII wordmark;
- native Pi version beneath it;
- workspace path;
- configured agent/model/thinking matrix;
- setup capabilities, commands, discovered skills, and shortcuts;
- no duplicate native loaded-resource dump.

Terminal Control session was stopped after capture.
