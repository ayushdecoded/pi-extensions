# Settled panel expiry

`expired.*` verifies that an old settled batch no longer occupies the inline panel. Only the muted `See all agent batches in /agents` hint remains. Active batches still show live rows, and newly settled summaries remain visible for 45 seconds.

Terminal Control session was stopped after capture.
